# FastAPI backend for voice cloning
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
soundfile==0.12.1
numpy<2.0
yt-dlp                      # YouTube link theke reference audio ana

# TTS engines
coqui-tts                   # Coqui XTTS-v2 (maintained fork; Python 3.12 compatible)
chatterbox-tts             # base for BanglaTTS adapter
resemble-perth             # `import perth` watermarker (Chatterbox dependency)
huggingface_hub            # download Banglabox/chatterbox-bangla-tts
peft                       # LoRA adapter load (BanglaTTS)
torch>=2.1                 # GPU build recommended (Colab). CPU o cholе (slow).
torchaudio>=2.1
