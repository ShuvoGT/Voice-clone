# FastAPI backend for voice cloning
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
soundfile==0.12.1
librosa                     # audio convert fallback

# TTS — SINGLE stack (Chatterbox) so deps don't conflict:
#   English + 20+ languages  -> ChatterboxMultilingualTTS
#   Bangla (fine-tuned)      -> Banglabox/chatterbox-bangla-tts (BanglaTTS)
chatterbox-tts
resemble-perth              # `import perth` watermarker (Chatterbox dependency)
peft                        # LoRA adapter load (BanglaTTS)
huggingface_hub             # download Banglabox model
torch>=2.1
torchaudio>=2.1

yt-dlp                      # YouTube link theke reference audio ana
