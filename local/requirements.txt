# Local (laptop/CPU) test er jonno. Python 3.10 or 3.11 recommended.
# Install:  pip install -r requirements.txt
# Note: CPU te XTTS slow (ek line ~1-2 min). Serious kaj Colab e koro.

TTS==0.22.0          # Coqui TTS (XTTS-v2 included)
torch>=2.1,<2.4      # CPU build thik ache; GPU thakle CUDA build install koro
soundfile
